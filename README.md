# Media Metadata ETL Pipeline
This project demonstrates the design and implementation of an end-to-end ETL pipeline using Python and PostgreSQL. Raw media metadata is extracted from multiple CSV sources, validated and transformed into a standardized schema, loaded into PostgreSQL, and queried using SQL to support downstream analytics.
